import Layout from "../../layouts/default"
import BookForm from "../../components/Book"
const New = () => {
    return (
        <Layout>
            <BookForm book={null} />
        </Layout>
    );
}
export default New;